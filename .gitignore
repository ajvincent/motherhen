# a temporary directory for test-build checkouts
test/checkouts

# an object directory for test-compiled Mozilla code
test/objdir

node_modules

cli/**/*.mjs.map
cli/**/*.d.mts
cli/**/*.js.map
cli/**/*.d.ts


# A directory for mozilla-unified to live in as a repository with no patches.
.cleanroom

# the default location to configure a project
.motherhen-config.json

# Disabled per user request.
!.motherhen-config.json
